import { StyleSheet } from 'react-native';

export const getFormStyles = (isDark: boolean) => StyleSheet.create({
  container: {
    flex: 1,
  },
  scrollContent: {
    paddingBottom: 40,
    padding: 24,
  },